import { Request, Response, NextFunction } from 'express';
// import { userPostSchema } from '../helpers/schemes';

// function productPostVal(req: Request, res: Response, next: NextFunction) {
  // const product = req.body;
  // const { error } = productPostSchema.validate(product);

  // if (error) {
    // const status = error.details[0].type === 'any.required' ? 400 : 422;
    // return res.status(status).json({ message: error.details[0].message });
  // }

  // next();
// }

// export {
  // productPostVal,
// }; 